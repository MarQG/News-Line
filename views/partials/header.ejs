<!DOCTYPE html>
<html>
    <head>
        <title>News Line | Keep your finger on the pulse</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link href="https://fonts.googleapis.com/css?family=Anton|Teko:300" rel="stylesheet">
        <link rel="stylesheet" href="/stylesheets/main.css" ></link>
    </head>
    <body>
        <nav class="navbar navbar-inverse ">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="/dashboard">NEWS LINE</a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        
                        <% if(!currentUser){ %>
                            <li><a class="navbar-text" href="/login">Login</a></li>
                            <li><a class="navbar-text" href="/register">Sign Up</a></li>
                        <% } else { %>
                            <% if(hideForm) {%>
                                <li><a class="navbar-text" href="/dashboard">Back To Dashbaord</a></li>
                            <% } else { %>
                                <li><a class="navbar-text" href="/articles">Saved Articles</a></li>
                                <li>
                                        <form id="scraper-form" class="form-inline">
                                            <div class="form-group">
                                                <label class="sr-only">News Site</label>
                                                <select class="form-control">
                                                    <option>Huffington Post</option>
                                                    <option>New York Times</option>
                                                    <option>CNN</option>
                                                </select>
                                            </div>
                                            <button type="submit" class="btn btn-primary">Scrape New Articles</button>
                                        </form>
                                    </li>
                            <% } %>
                            <li></li>
                            <li><a class="navbar-text" href="#">Signed In As <%= currentUser.username%></a></li>
                            <li><a class="navbar-text" href="/logout">Logout</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
        
        <div class="container">
            <% if(error && error.length > 0){ %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length > 0){ %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>
        
        
    